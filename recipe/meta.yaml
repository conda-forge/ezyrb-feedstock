{% set name = "ezyrb" %}
{% set version = "1.3.0.post2212" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ezyrb-{{ version }}.tar.gz
  sha256: cfe8907a9a2c8240a3c44e9249967a6c4cb5dad8546dcb4ff0c06c7d58d07830

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - future
    - matplotlib-base
    - numpy
    - python >=3.6
    - scikit-learn >=1.0
    - scipy
    - pytorch

test:
  imports:
    - ezyrb
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/mathLab/EZyRB
  summary: Easy Reduced Basis
  license: MIT
  license_file: LICENSE.rst

extra:
  recipe-maintainers:
    - jan-janssen
